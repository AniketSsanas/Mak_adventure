[build]
  publish = "build"
  command = "CI=false DISABLE_ESLINT_PLUGIN=true react-scripts build"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
